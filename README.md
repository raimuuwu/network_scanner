# network_scanner
Simple network scanner CLI
Works on Linux and Windows

## Features:
- Displays network info
- Scans local network for hosts

## Usage
- "-i", "--info" -> display info
- "-s", "--scan" -> scan local network
- no parameters -> displays help

## Issues
When using scanner functionality on Windows there are multiple "ghost answers" from addresses that are not occupied. I might fix that later.
